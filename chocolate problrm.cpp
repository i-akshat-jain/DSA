#include <stdio.h>

int main() 
{
	int t;
	scanf("%d",&t);
	while(t--)
	{
		int i,n;
		scanf("%d",&n)
		for(i=0;i<n;i++)
		{
			scanf("%d",a[i]);
		}
		for(i=0;i<n;i++)
		{
			x = a[0]
			if(x > a[i-1])
			{
				for(i = x-1 ; i < n-1; i++)
    			    array[i] = array[i+1];
			}
			else
				
		}
	}
	return 0;
}
